@model LinhKienShop.Models.DanhMuc

@{
    ViewData["Title"] = "Sửa Danh Mục";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-4">
    <div class="card shadow">
        <div class="card-header bg-success text-white">
            <h2 class="mb-0 text-center"><i class="fas fa-edit me-2"></i><b>SỬA DANH MỤC SẢN PHẨM</b></h2>
        </div>
        <div class="card-body">
            <form asp-action="Sua" method="post">
                <input type="hidden" name="maDanhMuc" value="@Model.MaDanhMuc" />

                <div class="mb-3">
                    <label class="form-label fw-bold">Mã danh mục</label>
                    <input class="form-control" value="@Model.MaDanhMuc" disabled />
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold">Tên danh mục</label>
                    <input name="tenDanhMuc" class="form-control" value="@Model.TenDanhMuc" />
                    @if (ViewData.ModelState["tenDanhMuc"]?.Errors.Any() == true)
                    {
                        <span class="text-danger">@ViewData.ModelState["tenDanhMuc"].Errors.First().ErrorMessage</span>
                    }
                </div>

                <div class="d-flex justify-content-start gap-2">
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-save me-1"></i>Lưu
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-arrow-left me-1"></i>Quay lại
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>